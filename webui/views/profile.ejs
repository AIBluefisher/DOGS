<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Manage your 3DGS Viewer profile">
    <title>Profile Settings - 3DGS Viewer</title>
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="./static/fonts/font-awesome-4.7.0/css/font-awesome.min.css">
    
    <!-- Custom Styles -->
    <style>
        :root {
            --primary-color: #6366f1;
            --primary-dark: #4f46e5;
            --secondary-color: #10b981;
            --dark-bg: #111827;
            --light-bg: #f9fafb;
            --text-primary: #1f2937;
            --text-secondary: #6b7280;
            --card-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
            --hover-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.15);
            --transition: all 0.3s ease;
            --success-color: #10b981;
            --error-color: #ef4444;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #f9fafb;
            min-height: 100vh;
        }

        /* Navigation (same as index.ejs) */
        .navbar {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .navbar-container {
            display: grid;
            grid-template-columns: 1fr auto 1fr;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            text-decoration: none;
            color: var(--text-primary);
            font-weight: 700;
            font-size: 1.5rem;
            justify-self: center;
        }

        .logo img {
            height: 40px;
            width: 40px;
            object-fit: contain;
        }

        .nav-links {
            justify-self: end;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .nav-link {
            text-decoration: none;
            color: var(--text-secondary);
            font-weight: 500;
            transition: var(--transition);
            padding: 0.5rem 1rem;
            border-radius: 0.5rem;
        }

        .nav-link:hover {
            color: var(--primary-color);
            background: rgba(99, 102, 241, 0.1);
        }

        .btn {
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            font-weight: 600;
            text-decoration: none;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            border: none;
            cursor: pointer;
            font-family: 'Inter', sans-serif;
        }

        .btn-primary {
            background: var(--primary-color);
            color: white;
        }

        .btn-primary:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: var(--hover-shadow);
        }

        .btn-outline {
            background: transparent;
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
        }

        .btn-outline:hover {
            background: var(--primary-color);
            color: white;
        }

        .btn-danger {
            background: var(--error-color);
            color: white;
            border: 2px solid var(--error-color);
        }

        .btn-danger:hover {
            background: #dc2626;
            border-color: #dc2626;
        }

        /* Profile Container */
        .profile-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .profile-header {
            margin-bottom: 3rem;
            text-align: center;
        }

        .profile-header h1 {
            font-size: 2.5rem;
            color: var(--text-primary);
            margin-bottom: 0.5rem;
        }

        .profile-header p {
            color: var(--text-secondary);
            font-size: 1.125rem;
        }

        /* Profile Layout */
        .profile-layout {
            display: grid;
            grid-template-columns: 300px 1fr;
            gap: 2rem;
        }

        @media (max-width: 768px) {
            .profile-layout {
                grid-template-columns: 1fr;
            }
        }

        /* Sidebar */
        .profile-sidebar {
            background: white;
            border-radius: 1rem;
            padding: 2rem;
            box-shadow: var(--card-shadow);
            height: fit-content;
            position: sticky;
            top: 100px;
        }

        .avatar-section {
            text-align: center;
            margin-bottom: 2rem;
        }

        .avatar-preview {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            margin: 0 auto 1rem;
            border: 4px solid white;
            box-shadow: var(--card-shadow);
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 3rem;
            font-weight: 600;
        }

        .avatar-upload {
            margin-top: 1rem;
        }

        .avatar-upload input[type="file"] {
            display: none;
        }

        .avatar-upload label {
            display: inline-block;
            padding: 0.5rem 1rem;
            background: var(--light-bg);
            border: 1px dashed #d1d5db;
            border-radius: 0.5rem;
            cursor: pointer;
            color: var(--text-secondary);
            font-size: 0.875rem;
            transition: var(--transition);
        }

        .avatar-upload label:hover {
            border-color: var(--primary-color);
            color: var(--primary-color);
        }

        .user-stats {
            display: grid;
            gap: 1rem;
            margin-top: 2rem;
        }

        .stat-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.75rem;
            background: var(--light-bg);
            border-radius: 0.5rem;
        }

        .stat-label {
            color: var(--text-secondary);
            font-size: 0.875rem;
        }

        .stat-value {
            font-weight: 600;
            color: var(--text-primary);
        }

        /* Main Content */
        .profile-content {
            background: white;
            border-radius: 1rem;
            padding: 2rem;
            box-shadow: var(--card-shadow);
        }

        .section {
            margin-bottom: 2.5rem;
        }

        .section:last-child {
            margin-bottom: 0;
        }

        .section h2 {
            font-size: 1.5rem;
            color: var(--text-primary);
            margin-bottom: 1.5rem;
            padding-bottom: 0.75rem;
            border-bottom: 2px solid var(--light-bg);
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .section h2 i {
            color: var(--primary-color);
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            font-size: 0.875rem;
            font-weight: 500;
            color: var(--text-primary);
            margin-bottom: 0.5rem;
        }

        .form-control {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1px solid #d1d5db;
            border-radius: 0.5rem;
            font-family: 'Inter', sans-serif;
            font-size: 0.875rem;
            transition: var(--transition);
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
        }

        textarea.form-control {
            min-height: 100px;
            resize: vertical;
        }

        .form-help {
            display: block;
            font-size: 0.75rem;
            color: var(--text-secondary);
            margin-top: 0.25rem;
        }

        /* Messages */
        .upload-message {
            margin-top: 1rem;
            padding: 0.75rem;
            border-radius: 0.5rem;
            font-size: 0.875rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            animation: fadeIn 0.3s ease;
        }

        .messages {
            margin-bottom: 2rem;
        }

        .message {
            padding: 1rem;
            border-radius: 0.5rem;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .message-success {
            background: rgba(16, 185, 129, 0.1);
            border: 1px solid rgba(16, 185, 129, 0.2);
            color: var(--success-color);
        }

        .message-error {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.2);
            color: var(--error-color);
        }

        /* Avatar Actions */
        .avatar-actions {
            display: flex;
            gap: 0.5rem;
            margin-top: 1rem;
            justify-content: center;
        }

        .btn-sm {
            padding: 0.5rem 1rem;
            font-size: 0.875rem;
        }

        /* Danger Zone */
        .danger-zone {
            border: 2px solid rgba(239, 68, 68, 0.2);
            border-radius: 0.75rem;
            padding: 1.5rem;
            background: rgba(239, 68, 68, 0.05);
        }

        .danger-zone h3 {
            color: var(--error-color);
            margin-bottom: 1rem;
            font-size: 1.125rem;
        }

        .danger-zone p {
            color: var(--text-secondary);
            margin-bottom: 1rem;
            font-size: 0.875rem;
        }

        /* Back link */
        .back-link {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--text-secondary);
            text-decoration: none;
            margin-bottom: 2rem;
            font-weight: 500;
            transition: var(--transition);
        }

        .back-link:hover {
            color: var(--primary-color);
        }

        /* Footer */
        footer {
            background: rgba(0, 0, 0, 0.8);
            color: rgba(255, 255, 255, 0.7);
            padding: 3rem 2rem;
            text-align: center;
            margin-top: 4rem;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .footer-links a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
        }

        .footer-links a:hover {
            color: white;
        }
    </style>
</head>
<body>

    <!-- Navigation -->
    <nav class="navbar">
        <div class="navbar-container">
            <!-- Left empty column -->
            <div></div>
            
            <!-- Centered logo -->
            <a href="/" class="logo">
                <img src="./static/images/dogs.png" alt="3DGS Viewer Logo">
                <span>3DGS Viewer</span>
            </a>
            
            <!-- Right-aligned nav-links -->
            <div class="nav-links">
                <a href="/" class="nav-link">
                    <i class="fa fa-home"></i> Home
                </a>
                <a href="/upload" class="nav-link">
                    <i class="fa fa-upload"></i> Upload
                </a>
                <% if (user) { %>
                    <div class="user-menu" style="position: relative;">
                        <div class="user-avatar" onclick="toggleDropdown()" style="width: 40px; height: 40px; border-radius: 50%; background: var(--primary-color); color: white; display: flex; align-items: center; justify-content: center; font-weight: 600; cursor: pointer;">
                            <% if (user.avatar_path) { %>
                                <img src="<%= user.avatar_path %>" alt="<%= user.username %>" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">
                            <% } else { %>
                                <%= user.username.charAt(0).toUpperCase() %>
                            <% } %>
                        </div>
                        <div class="dropdown-menu" id="dropdownMenu" style="position: absolute; top: 100%; right: 0; background: white; border-radius: 0.75rem; box-shadow: var(--hover-shadow); padding: 0.5rem; min-width: 200px; display: none; z-index: 1001;">
                            <span class="dropdown-item" style="display: block; padding: 0.75rem 1rem; color: var(--text-primary); text-decoration: none; border-radius: 0.5rem; font-weight: 600;">
                                <i class="fa fa-user"></i> <%= user.username %>
                            </span>
                            <hr style="margin: 0.5rem 0; border-color: rgba(0,0,0,0.1);">
                            <a href="/profile" class="dropdown-item" style="display: block; padding: 0.75rem 1rem; color: var(--text-primary); text-decoration: none; border-radius: 0.5rem; transition: var(--transition);">
                                <i class="fa fa-cog"></i> Profile Settings
                            </a>
                            <a href="/upload" class="dropdown-item" style="display: block; padding: 0.75rem 1rem; color: var(--text-primary); text-decoration: none; border-radius: 0.5rem; transition: var(--transition);">
                                <i class="fa fa-upload"></i> Upload Model
                            </a>
                            <a href="/delete" class="dropdown-item" style="display: block; padding: 0.75rem 1rem; color: var(--text-primary); text-decoration: none; border-radius: 0.5rem; transition: var(--transition);">
                                <i class="fa fa-trash"></i> Manage Models
                            </a>
                            <hr style="margin: 0.5rem 0; border-color: rgba(0,0,0,0.1);">
                            <form action="/logout" method="post" style="margin: 0;">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <button type="submit" class="dropdown-item" style="width: 100%; text-align: left; background: none; border: none; cursor: pointer; padding: 0.75rem 1rem; color: var(--text-primary); font-family: 'Inter', sans-serif; font-size: 1rem;">
                                    <i class="fa fa-sign-out"></i> Sign Out
                                </button>
                            </form>
                        </div>
                    </div>
                <% } else { %>
                    <a href="/login" class="nav-link">Login</a>
                    <a href="/signup" class="btn btn-primary">Sign Up</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Profile Container -->
    <div class="profile-container">
        <!-- Back Link -->
        <a href="/" class="back-link">
            <i class="fa fa-arrow-left"></i> Back to Home
        </a>

        <!-- Profile Header -->
        <div class="profile-header">
            <h1>Profile Settings</h1>
            <p>Manage your account information and preferences</p>
        </div>

        <!-- Messages -->
        <% if (hasMessages) { %>
            <div class="messages">
                <% messages.forEach(function(message) { %>
                    <div class="message message-<%= message.type || 'info' %>">
                        <i class="fa fa-<%= message.type === 'success' ? 'check-circle' : 'exclamation-circle' %>"></i>
                        <span><%= message.text %></span>
                    </div>
                <% }); %>
            </div>
        <% } %>

        <!-- Profile Layout -->
        <div class="profile-layout">
            <!-- Sidebar -->
            <div class="profile-sidebar">
<!-- Avatar Upload Form - SIMPLIFIED -->
<div class="avatar-section">
    <div class="avatar-preview">
        <% if (user.avatar_path) { %>
            <img src="<%= user.avatar_path %>" alt="<%= user.full_name || user.username %>" 
                 style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">
        <% } else if (user.email && gravatar) { %>
            <img src="<%= gravatar.getGravatarUrl(user.email, 150, 'identicon') %>" 
                 alt="<%= user.full_name || user.username %>"
                 style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">
        <% } else { %>
            <div style="width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; 
                       background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; font-size: 3rem; font-weight: 600;">
                <%= user.username.charAt(0).toUpperCase() %>
            </div>
        <% } %>
    </div>
    
    <h3 style="margin-bottom: 0.5rem;"><%= user.full_name || user.username %></h3>
    <p style="color: var(--text-secondary); font-size: 0.875rem;">@<%= user.username %></p>
    
    <!-- Avatar Upload Form - No CSRF token needed since we're skipping it -->
    <form action="/profile/avatar" method="post" enctype="multipart/form-data" id="avatarForm">
        <!-- NO CSRF TOKEN HERE since we're skipping CSRF for this route -->
        <input type="file" id="avatarInput" name="avatar" accept="image/*" 
            onchange="uploadAvatar(this)" style="display: none;">
        <button type="button" class="btn btn-outline btn-sm" onclick="document.getElementById('avatarInput').click()" 
                style="margin-top: 1rem;">
            <i class="fa fa-camera"></i> Change Avatar
        </button>
    </form>

    <% if (user.avatar_path) { %>
        <!-- DELETE form still needs CSRF token -->
        <form action="/profile/avatar/delete" method="post" style="margin-top: 0.5rem;">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <button type="submit" class="btn btn-danger btn-sm" 
                    onclick="return confirm('Are you sure you want to remove your avatar?')">
                <i class="fa fa-trash"></i> Remove Avatar
            </button>
        </form>
    <% } %>
</div>

<script>
function uploadAvatar(input) {
    if (input.files && input.files[0]) {
        // Show preview
        const reader = new FileReader();
        reader.onload = function(e) {
            const avatarPreview = document.querySelector('.avatar-preview');
            avatarPreview.innerHTML = `<img src="${e.target.result}" alt="Preview" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">`;
        };
        reader.readAsDataURL(input.files[0]);
        
        // Submit the form
        document.getElementById('avatarForm').submit();
    }
}
</script>

                <!-- User Stats -->
                <div class="user-stats">
                    <div class="stat-item">
                        <span class="stat-label">Models Uploaded</span>
                        <span class="stat-value"><%= modelsCount %></span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">Member Since</span>
                        <span class="stat-value">
                            <% if (user.created_at) { %>
                                <%= new Date(user.created_at).toLocaleDateString() %>
                            <% } else { %>
                                Recently
                            <% } %>
                        </span>
                    </div>
                </div>
            </div>

            <!-- Main Content -->
            <div class="profile-content">
                <!-- Profile Information Section -->
                <div class="section">
                    <h2><i class="fa fa-user"></i> Profile Information</h2>
                    
                    <form action="/profile/update" method="post">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        
                        <div class="form-group">
                            <label class="form-label">Username</label>
                            <input type="text" class="form-control" value="<%= user.username %>" disabled>
                            <small class="form-help">Username cannot be changed</small>
                        </div>
                        
                        <div class="form-group">
                            <label class="form-label">Full Name</label>
                            <input type="text" name="full_name" class="form-control" value="<%= user.full_name || '' %>" placeholder="Enter your full name">
                        </div>
                        
                        <div class="form-group">
                            <label class="form-label">Email Address</label>
                            <input type="email" name="email" class="form-control" value="<%= user.email || '' %>" placeholder="Enter your email address">
                            <small class="form-help">We'll never share your email with anyone else</small>
                        </div>
                        
                        <div class="form-group">
                            <label class="form-label">Bio</label>
                            <textarea name="bio" class="form-control" placeholder="Tell us a little about yourself"><%= user.bio || '' %></textarea>
                            <small class="form-help">Max 500 characters</small>
                        </div>
                        
                        <button type="submit" class="btn btn-primary">
                            <i class="fa fa-save"></i> Save Changes
                        </button>
                    </form>
                </div>

                <!-- Account Section -->
                <div class="section">
                    <h2><i class="fa fa-shield"></i> Account Security</h2>
                    <p style="color: var(--text-secondary); margin-bottom: 1.5rem;">Manage your password and account security settings</p>
                    
                    <div class="form-group">
                        <a href="/change-password" class="btn btn-outline">
                            <i class="fa fa-key"></i> Change Password
                        </a>
                        <small class="form-help">Update your account password</small>
                    </div>
                </div>

                <!-- Danger Zone -->
                <div class="section">
                    <h2><i class="fa fa-exclamation-triangle"></i> Danger Zone</h2>
                    <div class="danger-zone">
                        <h3>Delete Account</h3>
                        <p>Once you delete your account, there is no going back. This will permanently delete your account and remove all your data from our servers.</p>
                        <button class="btn btn-danger" onclick="showDeleteConfirmation()">
                            <i class="fa fa-trash"></i> Delete Account
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <div class="footer-links">
                <a href="/">Home</a>
                <a href="/about">About</a>
                <a href="/documentation">Documentation</a>
                <a href="/contact">Contact</a>
            </div>
            <p style="margin-bottom: 1rem;">&copy; 2024 3DGS Viewer. All rights reserved.</p>
            <p style="font-size: 0.875rem; opacity: 0.6;">Powered by 3D Gaussian Splatting technology</p>
        </div>
    </footer>

    <!-- Scripts -->
    <script>
        // Toggle dropdown menu
        function toggleDropdown() {
            const dropdown = document.getElementById('dropdownMenu');
            dropdown.style.display = dropdown.style.display === 'block' ? 'none' : 'block';
        }

        // Close dropdown when clicking outside
        document.addEventListener('click', function(event) {
            const dropdown = document.getElementById('dropdownMenu');
            const avatar = document.querySelector('.user-avatar');
            
            if (dropdown && avatar && !dropdown.contains(event.target) && !avatar.contains(event.target)) {
                dropdown.style.display = 'none';
            }
        });

        // Delete account confirmation
        function showDeleteConfirmation() {
            if (confirm('Are you sure you want to delete your account? This action cannot be undone.')) {
                alert('Account deletion would be processed here. This feature is not yet implemented.');
                // In a real implementation, you would submit a form to delete the account
                // document.getElementById('deleteAccountForm').submit();
            }
        }

        // Avatar preview
        document.getElementById('avatarInput')?.addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    const avatarPreview = document.querySelector('.avatar-preview');
                    avatarPreview.innerHTML = `<img src="${e.target.result}" alt="Preview" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">`;
                };
                reader.readAsDataURL(file);
            }
        });

    // Function to preview and submit avatar
    function previewAndSubmitAvatar(input) {
        if (input.files && input.files[0]) {
            // Validate file size (5MB limit)
            if (input.files[0].size > 5 * 1024 * 1024) {
                alert('File size exceeds 5MB limit. Please choose a smaller image.');
                input.value = ''; // Clear the file input
                return;
            }
            
            // Validate file type
            const allowedTypes = ['image/jpeg', 'image/png', 'image/gif', 'image/webp'];
            if (!allowedTypes.includes(input.files[0].type)) {
                alert('Only JPG, PNG, GIF, and WebP images are allowed.');
                input.value = ''; // Clear the file input
                return;
            }
            
            // Show preview
            const reader = new FileReader();
            reader.onload = function(e) {
                const avatarPreview = document.querySelector('.avatar-preview');
                avatarPreview.innerHTML = `<img src="${e.target.result}" alt="Preview" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">`;
            };
            reader.readAsDataURL(input.files[0]);
            
            // Show loading indicator
            const submitBtn = document.querySelector('.avatar-upload label');
            const originalHtml = submitBtn.innerHTML;
            submitBtn.innerHTML = '<i class="fa fa-spinner fa-spin"></i> Uploading...';
            submitBtn.disabled = true;
            
            // Submit the form
            setTimeout(() => {
                document.getElementById('avatarForm').submit();
            }, 100);
        }
    }

    // Function to handle AJAX avatar upload (alternative)
    function uploadAvatarWithAjax(file) {
        if (!file) return;
        
        // Validate file
        if (file.size > 5 * 1024 * 1024) {
            alert('File size exceeds 5MB limit.');
            return;
        }
        
        const allowedTypes = ['image/jpeg', 'image/png', 'image/gif', 'image/webp'];
        if (!allowedTypes.includes(file.type)) {
            alert('Only image files are allowed.');
            return;
        }
        
        // Show preview
        const reader = new FileReader();
        reader.onload = function(e) {
            const avatarPreview = document.querySelector('.avatar-preview');
            avatarPreview.innerHTML = `<img src="${e.target.result}" alt="Preview" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">`;
        };
        reader.readAsDataURL(file);
        
        // Create FormData
        const formData = new FormData();
        formData.append('avatar', file);
        formData.append('_csrf', '<%= csrfToken %>');
        
        // Show loading
        const submitBtn = document.querySelector('.avatar-upload label');
        const originalHtml = submitBtn.innerHTML;
        submitBtn.innerHTML = '<i class="fa fa-spinner fa-spin"></i> Uploading...';
        submitBtn.disabled = true;
        
        // Send request
        fetch('/profile/avatar', {
            method: 'POST',
            body: formData,
            headers: {
                'X-CSRF-Token': '<%= csrfToken %>'
            }
        })
        .then(response => {
            if (response.redirected) {
                // Server redirected, refresh the page
                window.location.href = response.url;
            } else if (response.ok) {
                return response.json().then(data => {
                    if (data.success) {
                        // Show success message
                        showMessage('Avatar updated successfully!', 'success');
                        // Reload after a delay
                        setTimeout(() => window.location.reload(), 1500);
                    } else {
                        throw new Error(data.message || 'Upload failed');
                    }
                });
            } else {
                throw new Error('Upload failed with status: ' + response.status);
            }
        })
        .catch(error => {
            console.error('Upload error:', error);
            showMessage('Failed to upload avatar: ' + error.message, 'error');
            submitBtn.innerHTML = originalHtml;
            submitBtn.disabled = false;
        });
    }

    // Helper function to show messages
    function showMessage(text, type) {
        // Remove existing messages
        const existingMsg = document.querySelector('.upload-message');
        if (existingMsg) existingMsg.remove();
        
        // Create message element
        const message = document.createElement('div');
        message.className = `upload-message message message-${type}`;
        message.innerHTML = `
            <i class="fa fa-${type === 'success' ? 'check-circle' : 'exclamation-circle'}"></i>
            <span>${text}</span>
        `;
        
        // Add to page
        const avatarSection = document.querySelector('.avatar-section');
        avatarSection.appendChild(message);
        
        // Remove after 5 seconds
        setTimeout(() => {
            if (message.parentNode) {
                message.remove();
            }
        }, 5000);
    }
    </script>
</body>
</html>
